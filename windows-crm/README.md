# Windows CRM Desktop Client

Recommended implementation: Electron + React + TypeScript.

The client consumes the backend endpoints described in `../docs/crm-api-contract.md`. Later modules: agent inbox, customer 360 timeline, live chat takeover, ticket actions, notes, tags and CRM reporting.
